# 合成大西瓜 - 增强版

## 项目结构

```
daxigua/
├── shared/js/           # 核心代码（唯一代码源）
│   ├── config.js        # 游戏配置
│   ├── physics.js       # 物理引擎
│   ├── renderer.js      # 渲染器
│   ├── game.js          # 游戏主逻辑
│   ├── soundSystem.js   # 音效系统
│   └── platform.js      # 平台适配层
├── index.html           # Web 版本入口
├── wechat-minigame/     # 微信小程序版本
│   ├── js/              # 从 shared/js 同步的代码
│   └── game.js          # 小程序入口
├── react-app/           # React 版本（可选）
└── res/                 # 资源文件
```

## 单一代码源策略

核心游戏逻辑存放在 `shared/js/` 目录，发布到各平台前需要同步：

```bash
# 同步代码到小程序目录
pnpm sync:weapp

# 构建并上传小程序
pnpm upload:weapp
```

## 功能特性

- 🎮 Combo 连击系统
- 🔥 Fever 狂热模式
- 🌤️ 天气系统（大风、梅雨、霜冻）
- ⚠️ 地震预警机制
- 🎁 盲盒果实
- 💣 炸弹水果
- 🧊 冰封果实
- ✨ Buff 系统

## 开发

```bash
# 安装依赖
pnpm install

# 启动 Web 版本
pnpm dev

# 同步并上传小程序
pnpm upload:weapp
```